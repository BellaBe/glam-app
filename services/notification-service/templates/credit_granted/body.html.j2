<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Credits Added</title>
</head>
<body style="font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Inter,Arial,sans-serif;line-height:1.6;color:#101828;">
  {{ header | safe }}

  {% from "shared/macros.html.j2" import divider, button with context %}

  <div style="padding:20px;max-width:600px;margin:0 auto;">
    <h1 style="margin:0 0 12px 0; font-size:20px; line-height:1.3; color:#344054;">Your trial is activeCredits Added</h1>

    <p style="margin:0 0 8px 0;">Hi {{ domain }},</p>
    <p style="margin:0 0 8px 0;"><strong>{{ amount }}</strong> credits have been added to your account.</p>
    <p style="margin:0 0 8px 0;">Your new balance is <strong>{{ new_balance }}</strong> credits.</p>
    <p style="margin:12px 0 0 0;color:#667085;">1 credit = 1 recommendation.</p>

    {{ divider() }}

    <ul style="margin:0;padding-left:18px;color:#475467;">
      <li>Recommendations continue without interruption</li>
      <li>Track usage in your dashboard</li>
      <li>Set alerts to avoid running out</li>
    </ul>

    {% if app_url is defined %}
      <div style="text-align:center;margin:24px 0 0 0;">
        {{ button(app_url ~ '/dashboard', 'Open Dashboard') }}
      </div>

      <div style="background:#F9FAFB;border:1px solid #E4E7EC;border-radius:8px;padding:12px 14px;margin:16px 0 0 0;font-size:14px;color:#475467;">
        <strong>Tip:</strong> Enable auto-recharge so recommendations never pause.
        <a href="{{ app_url }}/billing/auto-recharge" style="color:{{ brand_color | default('#BD4267') }};text-decoration:underline;">Set up auto-recharge →</a>
      </div>
    {% endif %}
  </div>

  {{ footer | safe }}
</body>
</html>
