<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Trial Credits Granted</title>
</head>
<body style="font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Inter,Arial,sans-serif; line-height:1.6; color:#101828;">
  {{ header | safe }}

  {% from "shared/macros.html.j2" import divider, button with context %}

  <div style="padding:20px; max-width:600px; margin:0 auto;">
    <h1 style="margin:0 0 12px 0; font-size:20px; line-height:1.3; color:#344054;">Your trial is active</h1>

    <p style="margin:0 0 8px 0;">Hi {{ domain }},</p>

    <p style="margin:0 0 8px 0;">
      Great news — your trial credits are ready to use. You’ve received <strong>{{ amount }}</strong> credits.
    </p>
    <p style="margin:0 0 8px 0; color:#667085;">1 credit = 1 fit &amp; color recommendation.</p>

    {{ divider() }}

    <h2 style="margin:12px 0 6px 0; font-size:16px; line-height:1.4; color:#344054;">What you can do</h2>
    <ul style="margin:0 0 12px 18px; padding:0; color:#475467;">
      <li>Enable fit &amp; color recommendations on your products</li>
      <li>Let shoppers get confident picks before purchase</li>
      <li>Track engagement and conversions in your dashboard</li>
    </ul>

    <h2 style="margin:12px 0 6px 0; font-size:16px; line-height:1.4; color:#344054;">Quick start</h2>
    <ol style="margin:0 0 12px 20px; padding:0; color:#475467;">
      <li>Sync your product catalog</li>
      <li>Configure display rules &amp; credit alerts</li>
      <li>Go live and test on your store</li>
      <li>Monitor results in the dashboard</li>
    </ol>

    {% if app_url is defined %}
      <div style="text-align:center; margin:20px 0 0 0;">
        {{ button(app_url ~ '/dashboard', 'Go to dashboard') }}
      </div>
    {% endif %}

    <p style="margin:16px 0 0 0; color:#667085; font-size:14px;">
      Trial credits don’t expire, but we recommend using them within 30 days to see impact quickly.
    </p>

    <p style="margin:12px 0 0 0;">
      Need help getting started?
      {% if app_url is defined %}
        <a href="{{ app_url }}/docs" style="color:{{ brand_color | default('#BD4267') }}; text-decoration:underline;">Read the docs</a>
      {% endif %}
      or email <a href="mailto:{{ support_email }}" style="color:{{ brand_color | default('#BD4267') }}; text-decoration:underline;">{{ support_email }}</a>.
    </p>

    {{ divider() }}
  </div>

  {{ footer | safe }}
</body>
</html>
