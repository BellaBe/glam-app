<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Trial Ended</title>
</head>
<body style="font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Inter,Arial,sans-serif; line-height:1.6; color:#101828;">
  {{ header | safe }}

  {% from "shared/macros.html.j2" import divider, button with context %}

  <div style="padding:20px; max-width:600px; margin:0 auto;">
    <h1 style="margin:0 0 12px 0; font-size:20px; line-height:1.3; color:#344054;">Your trial has ended</h1>

    <p style="margin:0 0 8px 0;">Hi {{ domain }},</p>

    <p style="margin:0 0 8px 0;">
      Your trial credits{% if exhausted_at is defined %} were exhausted on {{ exhausted_at | format_date }}{% endif %}.
      Fit &amp; color recommendations are currently <strong>paused</strong>.
    </p>

    {{ divider() }}

    <h2 style="margin:12px 0 6px 0; font-size:16px; line-height:1.4; color:#344054;">What this means</h2>
    <ul style="margin:0 0 12px 18px; padding:0; color:#475467;">
      <li>Recommendation widgets are hidden</li>
      <li>Shoppers can’t get fit &amp; color suggestions</li>
      <li>Analytics won’t update until you add credits</li>
    </ul>

    {% if app_url is defined %}
      <div style="text-align:center; margin:20px 0 0 0;">
        {{ button(app_url ~ '/billing/plans', 'Choose a plan') }}
      </div>
      <p style="margin:12px 0 0 0; text-align:center;">
        <a href="{{ app_url }}/billing/credits" style="color:{{ brand_color | default('#BD4267') }}; text-decoration:underline;">
          Or purchase credits as you go →
        </a>
      </p>
    {% endif %}

    {% if discount_code is defined %}
      <div style="margin:16px 0 0 0; background:#F9FAFB; border:1px solid #E4E7EC; border-radius:8px; padding:12px 14px;">
        <strong>🎁 Special offer:</strong>
        <span>Get {{ discount_pct | default('20%') }} off your first month.</span>
        <span>Code: <span style="font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,'Liberation Mono','Courier New',monospace;">{{ discount_code }}</span></span>
      </div>
    {% endif %}

    <p style="margin:16px 0 0 0;">
      Questions? <a href="mailto:{{ support_email }}" style="color:{{ brand_color | default('#BD4267') }}; text-decoration:underline;">{{ support_email }}</a>
    </p>

    {{ divider() }}
  </div>

  {{ footer | safe }}
</body>
</html>
